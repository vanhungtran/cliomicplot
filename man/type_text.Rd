% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type_text.R
\name{type_text}
\alias{type_text}
\title{Text/Label Plot Type}
\usage{
type_text(
  labels = NULL,
  size = 3.5,
  alpha = 0.9,
  style = c("text", "label"),
  repel = TRUE,
  label.padding = 0.25,
  label.size = 0.25
)
}
\arguments{
\item{labels}{Character vector of text labels, or column name in data}

\item{size}{Text size (default 3.5)}

\item{alpha}{Text transparency}

\item{style}{"text" for plain text or "label" for text with background box}

\item{repel}{Use ggrepel to avoid overlapping (default TRUE)}

\item{label.padding}{Padding around label text when style="label"}

\item{label.size}{Border size for label boxes}
}
\value{
A cliplot_type object for use with \code{\link{cliplot}}.
}
\description{
Adds text labels at (x, y) positions. Supports both plain text
  and label-style (with background rectangle). Useful for volcano plots,
  scatter plot annotations, and point labeling.
}
\examples{
\dontrun{
# Label top points in a scatter
top_cars <- head(mtcars[order(-mtcars$mpg), ], 5)
cliplot(mpg ~ wt, data = mtcars, type = "points")
cliplot(mpg ~ wt, data = top_cars, type = "text", add = TRUE)

# Label with repel
cliplot(mpg ~ wt, data = mtcars,
        type = type_text(style = "label", repel = TRUE))
}

}
