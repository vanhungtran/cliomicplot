% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type_infobar.R
\name{type_infobar}
\alias{type_infobar}
\title{Infographic Bar Chart}
\usage{
type_infobar(
  bar_colors = NULL,
  box_colors = NULL,
  box_labels = NULL,
  box_sub_labels = NULL,
  reference_line = NULL,
  reference_label = NULL,
  value_labels = TRUE,
  value_suffix = "",
  change_labels = NULL,
  bar_width = 0.85,
  label_offset = 0.06,
  font_family = "sans",
  bg_color = "white"
)
}
\arguments{
\item{bar_colors}{Named character vector mapping category labels to bar fill
colours. If \code{NULL}, defaults to the active qualitative palette.}

\item{box_colors}{Named character vector mapping category labels to footer
box fill colours. If \code{NULL}, no footer boxes are drawn.}

\item{box_labels}{Character vector of labels to draw inside each footer box,
in the same order as the categories on the x-axis (default: the category
names themselves). Pass \code{NULL} to suppress box text.}

\item{box_sub_labels}{Character vector of secondary labels to draw below
each footer box (default \code{NULL}).}

\item{reference_line}{Numeric y-axis value at which to draw a horizontal
reference line (default \code{NULL} = no line).}

\item{reference_label}{Optional label to place next to the reference line.}

\item{value_labels}{Logical. Whether to draw the bar value as a label above
each bar (default \code{TRUE}).}

\item{value_suffix}{A string appended to each value label, e.g. \code{"\%"}
(default \code{""}).}

\item{change_labels}{Character vector of change annotations to draw below
the value labels, in category order (default \code{NULL}).}

\item{bar_width}{Width of bars as a fraction of the category spacing
(default 0.85).}

\item{label_offset}{Distance above each bar for value/change labels,
expressed as a fraction of the maximum bar value (default 0.06).}

\item{font_family}{Base font family for all text (default \code{"sans"}).}

\item{bg_color}{Background colour for the plot (default \code{"white"}).}
}
\value{
A \code{cliplot_type} object for use with \code{\link{cliplot}}.
}
\description{
Creates a publication or media-ready bar chart with per-bar custom colours,
optional footer boxes with labels, reference lines, and value annotations
above each bar. Designed for polling, survey, and any comparison where each
category carries its own brand or semantic colour.
}
\section{Formula convention}{

Call as \code{cliplot(value ~ category, data = df, type = "infobar",
bar_colors = c(CatA = "#FF0000", CatB = "#00FF00"))}. Colours and labels
are matched to categories by name (for named vectors) or by position
(for unnamed vectors).
}

\examples{
\dontrun{
polls <- data.frame(party = c("CDU", "SPD", "AfD"), poll = c(17, 13, 18))
cliplot(poll ~ party, data = polls, type = "infobar",
        bar_colors = c(CDU = "#55B1BE", SPD = "#EC0016", AfD = "#119CD2"),
        value_suffix = "\%", reference_line = 5)
}

}
