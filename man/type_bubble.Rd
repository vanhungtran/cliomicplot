% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type_bubble.R
\name{type_bubble}
\alias{type_bubble}
\title{Bubble Chart Type}
\usage{
type_bubble(
  z = NULL,
  alpha = 0.65,
  scale_max = 15,
  stroke = 0.35,
  scale_size = TRUE
)
}
\arguments{
\item{z}{Variable or column name for the size dimension (bubble radius).
If NULL, tries to find a third numeric column in the data.}

\item{alpha}{Point transparency (0-1)}

\item{scale_max}{Maximum bubble size in mm (default 15)}

\item{stroke}{Point outline stroke width}

\item{scale_size}{Logical; apply sqrt scaling to area (default TRUE)}
}
\value{
A cliplot_type object for use with \code{\link{cliplot}}.
}
\description{
Creates bubble charts where point size encodes a third variable.
  Uses the \code{size} aesthetic in ggplot2. The size variable can be
  provided via the \code{z} parameter or via the formula interface
  as \code{z ~ x | by} with \code{y} passed separately.
}
\examples{
\dontrun{
# Bubble chart with built-in dataset
cliplot(mpg ~ wt, data = mtcars,
        type = type_bubble(z = mtcars$hp))

# Custom bubble scaling
cliplot(Sepal.Length ~ Sepal.Width, data = iris,
        type = type_bubble(z = iris$Petal.Length, scale_max = 20))
}

}
