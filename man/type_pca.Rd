% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type_pca.R
\name{type_pca}
\alias{type_pca}
\title{PCA Plot Type}
\usage{
type_pca(
  pc_x = 1,
  pc_y = 2,
  center = TRUE,
  scale. = TRUE,
  add_ellipse = TRUE,
  ellipse_level = 0.95,
  point_size = 3.2,
  point_alpha = 0.9,
  point_stroke = 0.45,
  label_samples = FALSE,
  label_size = 3,
  show_scree = FALSE,
  ellipse_alpha = 0.13,
  show_centroids = TRUE
)
}
\arguments{
\item{pc_x}{Principal component for x-axis (default 1)}

\item{pc_y}{Principal component for y-axis (default 2)}

\item{center}{Center data before PCA (default TRUE)}

\item{scale.}{Scale data before PCA (default TRUE)}

\item{add_ellipse}{Add confidence ellipses (default TRUE)}

\item{ellipse_level}{Confidence level for ellipses (default 0.95)}

\item{point_size}{Point size (default 3.2)}

\item{point_alpha}{Point transparency (default 0.9)}

\item{point_stroke}{Point outline width.}

\item{label_samples}{Label sample names (default FALSE)}

\item{label_size}{Label text size (default 3)}

\item{show_scree}{Show scree plot as inset (default FALSE)}

\item{ellipse_alpha}{Fill transparency for confidence ellipses.}

\item{show_centroids}{Add group centroid markers.}
}
\value{
A cliplot_type object for use with \code{\link{cliplot}}.
}
\description{
Creates publication-ready PCA/MDS plots for visualizing
  high-dimensional omics data. Supports ellipses, sample labeling, and
  scree plots.
}
\examples{
\dontrun{
# PCA of iris data
pca_data = iris[, 1:4]
cliplot(pca_data, type = "pca", by = iris$Species)

# Custom PCA
cliplot(pca_data, type = type_pca(add_ellipse = TRUE, label_samples = TRUE),
        by = iris$Species)

# From existing PCA results
pca_res = prcomp(iris[, 1:4], scale. = TRUE)
pca_scores = as.data.frame(pca_res$x)
pca_scores$Species = iris$Species
cliplot(PC2 ~ PC1, data = pca_scores, type = "points", by = Species)
}

}
